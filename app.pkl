import pickle
from sklearn.ensemble import RandomForestRegressor  # or whatever model you are using

# Assuming you have a model object named 'model'
model = RandomForestRegressor()  # replace with your actual model
# Train your model here if needed

with open('app.pkl', 'wb') as file:
    pickle.dump(model, file)

