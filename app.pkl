with open('app.pkl', 'rb') as model_file:
    try:
        loaded_model = pickle.load(model_file)
        print("Model loaded successfully:", loaded_model)
    except Exception as e:
        print("Error loading model:", e)

